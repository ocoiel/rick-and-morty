/**
 * A política de cache vive no core, junto do TTL que ela espelha: o header em
 * segundos e o TTL em milissegundos saem do mesmo número.
 */
export { PUBLIC_DAY_CACHE_CONTROL } from '@zrp/core';
